environments {
  dev {
    baseUrl = "https://six-skills.ru"
    scenario = "sherlock"
    intensity = 4
  }
  test {
    baseUrl = "https://reqres.in.test"
    scenario = "smoke"
    intensity = 4
  }
}

default {
  scenario = ${environments.dev.scenario}
}